{
  "name": "controle-adega-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "db:local": "wrangler d1 migrations apply controle-adega --local",
    "db:remote": "wrangler d1 migrations apply controle-adega --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}

